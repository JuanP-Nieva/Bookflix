@model Bookflix.Models.Reportes

@{
    ViewData["Title"] = "Create";
}

<h4 style="color: #F5F5F1;">  Escribenos tu reporte</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="hidden" asp-for="LibroId" value="@ViewBag.IdLibro" />
            </div>
            <div class="form-group">
                <input type="hidden" asp-for="PerfilId" value="@ViewBag.PerfilId" />
            </div>
            <div class="form-group">
                <label asp-for="Motivo" class="control-label" style="color: #F5F5F1;"></label>
                <input asp-for="Motivo" class="form-control" />
                <span asp-validation-for="Motivo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a class="btn btn-primary" role="button" asp-controller="Libro" asp-action="Index">Cancelar</a>
            </div>
        </form>
    </div>
</div>



@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
